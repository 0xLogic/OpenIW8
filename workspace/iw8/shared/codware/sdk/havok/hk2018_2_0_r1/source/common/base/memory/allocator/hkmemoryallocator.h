/*
==============
hkMemoryAllocator::resetPeakMemoryStatistics
==============
*/

void __fastcall hkMemoryAllocator::resetPeakMemoryStatistics(hkMemoryAllocator *this)
{
  ?resetPeakMemoryStatistics@hkMemoryAllocator@@UEAAXXZ(this);
}

/*
==============
hkMemoryAllocator::pushFrame
==============
*/

void __fastcall hkMemoryAllocator::pushFrame(hkMemoryAllocator *this)
{
  ?pushFrame@hkMemoryAllocator@@UEAAXXZ(this);
}

/*
==============
hkMemoryAllocator::advanceFrame
==============
*/

void __fastcall hkMemoryAllocator::advanceFrame(hkMemoryAllocator *this)
{
  ?advanceFrame@hkMemoryAllocator@@UEAAXXZ(this);
}

/*
==============
hkMemoryAllocator::getExtendedInterface
==============
*/

hkMemoryAllocator::ExtendedInterface *__fastcall hkMemoryAllocator::getExtendedInterface(hkMemoryAllocator *this)
{
  return ?getExtendedInterface@hkMemoryAllocator@@UEAAPEAUExtendedInterface@1@XZ(this);
}

/*
==============
hkMemoryAllocator::popFrame
==============
*/

void __fastcall hkMemoryAllocator::popFrame(hkMemoryAllocator *this)
{
  ?popFrame@hkMemoryAllocator@@UEAAXXZ(this);
}

/*
==============
hkMemoryAllocator::advanceFrame
==============
*/
void hkMemoryAllocator::advanceFrame(hkMemoryAllocator *this)
{
  ;
}

/*
==============
hkMemoryAllocator::popFrame
==============
*/
void hkMemoryAllocator::popFrame(hkMemoryAllocator *this)
{
  ;
}

/*
==============
hkMemoryAllocator::pushFrame
==============
*/
void hkMemoryAllocator::pushFrame(hkMemoryAllocator *this)
{
  ;
}

/*
==============
hkMemoryAllocator::getExtendedInterface
==============
*/
hkMemoryAllocator::ExtendedInterface *hkMemoryAllocator::getExtendedInterface(hkMemoryAllocator *this)
{
  return 0i64;
}

/*
==============
hkMemoryAllocator::resetPeakMemoryStatistics
==============
*/
void hkMemoryAllocator::resetPeakMemoryStatistics(hkMemoryAllocator *this)
{
  ;
}

